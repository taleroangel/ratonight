import 'package:flutter/material.dart';

class LightingServiceScreen extends StatelessWidget {
  const LightingServiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
